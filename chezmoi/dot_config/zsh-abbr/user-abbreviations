abbr "bud"="buildah bud"
abbr "createnv"="nix flake new -t github:nix-community/nix-direnv"
abbr "crflake"="nix flake new -t github:nix-community/nix-direnv"
abbr "dirall"="direnv allow"
abbr "dirrev"="direnv revoke"
abbr "envall"="direnv allow"
abbr "g"="git"
abbr "ga"="git add"
abbr "gac"="git add -A && git commit"
abbr "gbr"="git branch"
abbr "gc"="git commit"
abbr "gco"="git checkout"
abbr "gdf"="git diff"
abbr "gget"="ghq get"
abbr "ggr"="git grep"
abbr "gl"="git log"
abbr "glo"="git log --oneline"
abbr "gmr"="git merge"
abbr "gpl"="git pull"
abbr "gpu"="git push"
abbr "grb"="git rebase"
abbr "gsh"="git stash"
abbr "gst"="git status"
abbr "gsw"="git switch"
abbr "lg"="lazygit"
abbr "nixossw"="sudo nixos-rebuild switch --flake"
abbr "nixosts"="sudo nixos-rebuild test --flake"
abbr "p"="podman"
abbr "pimgs"="podman images"
abbr "ppl"="podman pull"
abbr "ppld"="podman pull docker.io/"
abbr "pps"="podman ps"
abbr "prun"="podman run"
abbr "venv"="source venv/bin/activate"
