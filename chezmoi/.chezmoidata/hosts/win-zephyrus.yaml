windows:
  hosts:
    win-zephyrus:
      packages:
        chocolatey:
          apps:
            sudo:
              - nvidia-app
              - nvidia-broadcast
            no_sudo:
        winget:
          preCmd:
          extraOptions:
          sources:
          apps:
            sudo:
              - Asus.ArmouryCrate
            no_sudo:
