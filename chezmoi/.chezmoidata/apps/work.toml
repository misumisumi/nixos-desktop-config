[apps.users.ach18207ao]

[apps.users.ach18207ao.delta]
[apps.users.ach18207ao.delta.git]
[apps.users.ach18207ao.delta.git.core]
paper = "delta"
[apps.users.ach18207ao.delta.git.interactive]
diffFilter = "delta --color-only"
[apps.users.ach18207ao.delta.git.delta]

[apps.users.ach18207ao.git]
[apps.users.ach18207ao.git.core]
fileMode = false
[apps.users.ach18207ao.git.settings.user]
email = "s.kobayashi@misumi-sumi.com"
name = "misumisumi"
[apps.users.ach18207ao.git.settings.alias]
tree = "log --graph --oneline --decorate --all"                                           # logのツリー表示
co-fixup = "commit --fixup"
push-f = "push --force-with-lease"                                                        # pushの強制 (ローカルが最新で無い場合失敗する)
rebase-i = "rebase -i --autosquash"                                                       #
branch-clear = "!git branch --merged origin/HEAD | grep -vE 'HEAD' | xargs git branch -d"
rebase-origin = "!git fetch origin --prune && git rebase origin/HEAD"
[apps.users.ach18207ao.git.signing]
key = "2C0E62B7117471A5"
format = "openpgp"
signByDefault = false

[apps.users.ach18207ao.lazygit]
paper = "delta --paging=never"

[apps.users.ach18207ao.opencode]
[apps.users.ach18207ao.opencode.permission]
edit = "allow"
glob = "allow"
grep = "allow"
read = "allow"
skill = "allow"
webfetch = "allow"
websearch = "allow"
question = "allow"

[apps.users.ach18207ao.mcp.mcpServers]
[apps.users.ach18207ao.mcp.mcpServers.github-mcp-server]
command = "github-mcp-server"
args = ["stdio"]
[apps.users.ach18207ao.mcp.mcpServers.github-mcp-server.env]
GITHUB_PERSONAL_ACCESS_TOKEN = "${GITHUB_PAT}"
[apps.users.ach18207ao.mcp.mcpServers.paper_search]
command = "paper-search-mcp" # wrapped command made by me
# [apps.users.ach18207ao.mcp.mcpServers.discord]
# command = "discord-mcp" # wrapped command made by me
[apps.users.ach18207ao.mcp.mcpServers.nixos]
command = "mcp-nixos" # wrapped command made by me
# [apps.users.ach18207ao.mcp.mcpServers.terraform]
# command = "terraform-mcp-server" # wrapped command made by me
[apps.users.ach18207ao.mcp.mcpServers.filesystem]
command = "mcp-server-filesystem"      # wrapped command made by me
args = ["~/Documents/", "~/Workspace"]
[apps.users.ach18207ao.mcp.mcpServers.git]
command = "mcp-server-git" # wrapped command made by me
[apps.users.ach18207ao.mcp.mcpServers.context7]
command = "context7-mcp" # wrapped command made by me
[apps.users.ach18207ao.mcp.mcpServers.context7.env]
CONTEXT7_API_KEY = "${CONTEXT7_API_KEY}"

[apps.users.ach18207ao.shell]
historyIgnore = [
    "builtin *",
    "cd *",
    "history *",
    "kill *",
    "ls *",
    "mkdir *",
    "pkill *",
    "rm *",
    # git commands
    "git branch *",
    "git checkout *",
    "git log *",
    "git pull *",
    "git status*",
    # zoxide
    "z *",
    "zi *",
]

[apps.users.ach18207ao.bash]
bashrcExtra = ""
initExtra = '''
function share_history {  # 以下の内容を関数として定義
    history -a  # .bash_historyに前回コマンドを1行追記
    history -c  # 端末ローカルの履歴を一旦消去
    history -r  # .bash_historyから履歴を読み込み直す
}
PROMPT_COMMAND="share_history; ${PROMPT_COMMAND}"  # 上記関数をプロンプト毎に自動実施
'''
logoutExtra = ""
profileExtra = ""
shellOptions = [
    "-histappend",
    "checkwinsize",
    "extglob",
    "globstar",
    "checkjobs",
]
[apps.users.ach18207ao.bash.sessionVariables]

[apps.users.ach18207ao.zsh]
extraFunctions = ["zargs", "zmv", "zcp", "zln"]
extraModules = ["attr"]
extraOptions = [
    "EXTENDED_GLOB",     # 拡張GRUBの有効化(^: 否定、~: 除外)
    "BARE_GLOB_QUAL",    # 条件付け検索の有効化
    "MAGIC_EQUAL_SUBST", # cmd a=/to/pathの時に補完を効かせる
]

envExtra = '''
# Compatibilize with bash
export HOSTNAME=$(hostname)
'''
initExtraFirst = '''
# Calc startup time
# zmodload zsh/zprof
# zprof
'''
initExtraBeforeCompInit = ""
initExtra = '''
# The plugin will auto execute this zvm_after_init function
setopt EXTENDED_GLOB         # 拡張GRUBの有効化(^: 否定、~: 除外)
setopt BARE_GLOB_QUAL        # 条件付け検索の有効化
setopt MAGIC_EQUAL_SUBST     # cmd a=/to/pathの時に補完を効かせる

set vi-cmd-mode-string "\1\e[?8c\2"
set vi-ins-mode-string "\1\e[?0c\2"
# For marlonrichert/zsh-autocomplete
zstyle -e ':autocomplete:history-search-backward:*' list-lines 'reply=$(( LINES / 2 ))'
zstyle ':autocomplete:*' min-delay 0.05
zstyle ':completion:*' remote-access no
zstyle ':completion:*' completer \
    _complete _complete:-fuzzy _correct _approximate _ignored
bindkey '\t' menu-select "$terminfo[kcbt]" menu-select
bindkey -M menuselect '\t' menu-complete "$terminfo[kcbt]" reverse-menu-complete
'''
[apps.users.ach18207ao.zsh.localVariables]
ZVM_VI_INSERT_ESCAPE_BINDKEY = "jj"
ZVM_VI_VISUAL_ESCAPE_BINDKEY = "jj"
ZVM_VI_OPPEND_ESCAPE_BINDKEY = "jj"
ZVM_LINE_INIT_MODE = "$ZVM_MODE_INSERT"
ZVM_LAZY_KEYBINDINGS = false
ABBR_QUIET = 1
JOB_QUEUE_TMPDIR = "${XDG_CACHE_HOME:-$HOME/.cache}/zsh-job-queue/"
ABBR_TMPDIR = "${XDG_CACHE_HOME:-$HOME/.cache}/zsh-abbr/"

[apps.users.ach18207ao.zsh.zinit]
[apps.users.ach18207ao.zsh.zinit.plugins]
"lucid blockf depth=1 light-mode" = [
    "atload'. ./zsh-autocomplete.plugin.zsh' marlonrichert/zsh-autocomplete",
]
"wait'0a' lucid blockf depth=1 light-mode" = [
    "atload'_zsh_autosuggest_start' zsh-users/zsh-autosuggestions",
    "atload'zvm_init; zvm_update_cursor' jeffreytse/zsh-vi-mode",
]
"wait'!0b' lucid blockf depth=1 light-mode" = [
    "zdharma-continuum/fast-syntax-highlighting",
]
"wait'!1a' lucid blockf depth=1 light-mode" = [
    "hlissner/zsh-autopair",
    "olets/zsh-abbr",
]
"wait'1c' lucid" = [
    "atload'. ./aichat.zsh' https://raw.githubusercontent.com/sigoden/aichat/refs/heads/main/scripts/completions/aichat.zsh",
]
[apps.users.ach18207ao.zsh.zinit.prezto]
pmodules = ["helper", "spectrum"]
[apps.users.ach18207ao.zsh.zinit.prezto.pmodulesWithModifier]
"wait'1c' lucid" = ["terminal", "directory"]
